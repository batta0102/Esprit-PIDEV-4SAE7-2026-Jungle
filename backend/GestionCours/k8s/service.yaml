apiVersion: v1
kind: Service
metadata:
  name: gestioncours
  labels:
    app: gestioncours
spec:
  type: ClusterIP
  selector:
    app: gestioncours
  ports:
    - name: http
      port: 8098
      targetPort: 8098
      protocol: TCP
